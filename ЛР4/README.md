Задача 1. Создать функцию safe_division_list(lst), делящую 100 на элементы списка. Ошибка → вывести, какой элемент вызвал проблему.
Задача 2: Модуль json_db.py: • безопасное чтение • автоматическое создание файла, если отсутствует • функция add(record) • функция find(key, value)